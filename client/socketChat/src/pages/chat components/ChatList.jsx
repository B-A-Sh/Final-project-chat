
const ChatList = () => {

    // const [ChatList, setChatList] = useState([{}])
return (
    <div className="chatList">
        <header >
            Chat list
        </header>
        <main>

            בדיקה בדיקה
        </main>
    </div>
  )
}

export default ChatList